import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.rrcapp.starter',
  appName: 'rrcapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
